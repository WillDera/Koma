<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompat required by local_auth / BiometricPrompt. -->
    <style name="LaunchTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <style name="NormalTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    <!-- AppCompat theme for SourcePreferencesActivity (PreferenceFragmentCompat).
         NoActionBar: activity hosts an explicit Toolbar so prefs never draw under it. -->
    <style name="Theme.Koma.SourcePreferences" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <item name="colorPrimary">#C44C4C</item>
        <item name="colorPrimaryDark">#A33C3C</item>
        <item name="colorAccent">#C44C4C</item>
        <item name="android:statusBarColor">#A33C3C</item>
    </style>
    <!-- Same chrome as SourcePreferences for interactive CF / captcha WebView. -->
    <style name="Theme.Koma.SourceWebView" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">#C44C4C</item>
        <item name="colorPrimaryDark">#A33C3C</item>
        <item name="colorAccent">#C44C4C</item>
        <item name="android:statusBarColor">#A33C3C</item>
    </style>
</resources>
