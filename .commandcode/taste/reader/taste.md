- For reader UI: implement seamless scrolling using mangayomi's flat-list-with-transition-pages pattern (PageData model with isTransitionPage flag, ChapterPreloadManager, ReaderMemoryManagement mixin) — not Navigator.pushReplacement. The flat-list approach where every item is self-aware of its chapter is preferred when feasible. Confidence: 0.70
- For chapter navigation (next/previous chapter logic): use position-based lookup in a reading-order sorted chapter list (matching by chapter URL), not index arithmetic (c.index == ch.index + 1). When extensions return chapters newest-first, sort by index DESC to produce reading order (oldest first), then use list[position ± 1] to find next/previous. This is mangayomi's pattern. Confidence: 0.85
- When the reader supports seamless scrolling through chapters, all UI elements that display chapter-specific information (app bar title, bottom bar chapter name, etc.) must read from the tracked current chapter state — not the initial chapter name passed as a widget argument — so they update when the user scrolls into a different chapter. Confidence: 0.75
- Mark a chapter as read when the user reaches or surpasses its last page (same page index check as mangayomi's setPageIndex). Confidence: 0.80
- For chapter list read/unread visual treatment: use a 2px-wide left vertical bar (accent color for unread, greyed-out for read) combined with greyed text for read chapters — not a dot indicator. This matches mangayomi's ChapterListTileWidget pattern. Confidence: 0.80
