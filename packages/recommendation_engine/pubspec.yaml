name: recommendation_engine
description: >
  Public stub of the recommendation engine API. Returns empty results.
  Release / private builds override this package via pubspec_overrides.yaml.
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
