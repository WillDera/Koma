# Optional local dependency_overrides (gitignored).
# Copy to pubspec_overrides.yaml only if you need to pin host packages.
#
# recommendation_engine comes from the public git dep in pubspec.yaml:
#   https://github.com/just-nibble/recommendation-engine

dependency_overrides:
  archive: ^4.0.7
  hooks: ^2.0.2
  native_toolchain_c: ^0.19.2
